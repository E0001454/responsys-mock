# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - responsys-mock

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read 

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v4 
        with:
          node-version: '22.x'

      - name: npm install, build, and test
        run: |
          npm install
          npm run build --if-present
        env: 
          VITE_API_URL_REAL: ${{ secrets.VITE_API_URL_REAL }}
          VITE_API_URL_MOCK: ${{ secrets.VITE_API_URL_MOCK }}
          VITE_USE_MOCK: ${{ secrets.VITE_USE_MOCK }}
          VITE_OKTA_ISSUER: ${{ secrets.VITE_OKTA_ISSUER }}
          VITE_OKTA_CLIENT_ID: ${{ secrets.VITE_OKTA_CLIENT_ID }}
          VITE_OKTA_REDIRECT_URI: ${{ secrets.VITE_OKTA_REDIRECT_URI }}

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: ./dist 

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write 
      contents: read 

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app
          path: ./dist 
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_3542BEE795C04768A20C798FFCCD6D65 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_4EDCE21B937442189F8300A6F4200DE7 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_042572AE16664B41B44422ABB43327C2 }}

      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'responsys-mock'
          slot-name: 'Production'
          package: ./dist 